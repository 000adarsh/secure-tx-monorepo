services:
  - type: web
    name: secure-tx-api
    runtime: node
    buildCommand: cd ../.. && pnpm install --frozen-lockfile && pnpm run build --filter=@repo/api
    startCommand: node dist/server.js
    envVars:
      - key: PORT
        value: 4000
      - key: CORS_ORIGIN
        scope: build
